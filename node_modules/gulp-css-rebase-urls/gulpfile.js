var gulp = require('gulp');
var cssRebaseUrls = require('./index.js');

gulp.task('default', function() {
    gulp.src('./css/**/*.css')
        .pipe(cssRebaseUrls({ root: './css' }))
        .pipe(gulp.dest('./dist/'))
});
